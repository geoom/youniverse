<%= form_for(@request) do |f| %>
    <% if @request.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@request.errors.count, "error") %> prohibited this request from being saved:</h2>

          <ul>
            <% @request.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <label for="request_name" class="">Nombre</label>
      <br>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <label for="request_about" class="">Descripción</label>
      <br>
      <%= f.text_area :about %>
    </div>
    <div class="field">
      <label for="request_image" class="">Imagen</label>
      <br>
      <%= f.file_field :image %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
