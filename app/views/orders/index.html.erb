<h1>Ordenes</h1>

<table>
  <thead>
  <tr>
    <th>#</th>
    <th>artículos</th>
    <th>estado del pago</th>
    <th>fecha de pago</th>
  </tr>
  </thead>

  <tbody>
  <% @orders.each do |order| %>
      <tr>
        <td><%= order.id %></td>
        <td><%= order.order_items.count %></td>
        <td><%= order.payment.status %></td>
        <td><%= order.payment.purchased_at %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<br>